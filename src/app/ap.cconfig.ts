import { environment } from '../environments/environment';

export const appConfig = {
    usermodule : environment.baseUrl + 'usermodule',
    maya : environment.baseUrl + 'maya',
    avatar : environment.avatar + '?background=random&name='
}